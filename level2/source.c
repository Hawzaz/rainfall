#include <stdio.h>

void	p(void)
{
	char buff[76];
	fflush(stdout);
	gets(buff);
	if ()
	{
		printf("0x804854a");
		strdup(
	}
	else
	{
		printf();
		exit(0);
	}
}

int		main()
{
	p();
	return (0);
}
