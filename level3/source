#include <stdio.h>

int m;

void	v(void)
{
	FILE *fp;
	char str[512];

	fgets(str, 512, fp);
	printf("%s\n", str);
	if (m = 64)
	{
		fwrite(fp, "Wait what?!\n", 11);
		syscall("\bin\sh");
	}
}

int		main()
{
	v();
	return (1);
}
