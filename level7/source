#include <stdio.h>
#include <stdlib.h>
#include <time.h>
void	m(void)
{
	time();
	printf("%s - %d\n", , c);
}

int		main(void)
{
	char s1,s2,s3;
	FILE *fp;

	s1 = malloc(8);
	s2 = malloc(8);
	s3 = malloc(8);
	strcpy(s1, s2);
	strcpy(s2, s3);
	fp = fopen("/home/user/level8/.pass", "r");
	fgets(c, 68, fp);
	puts("~~");
	return (1);
}
